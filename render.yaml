services:
  - type: worker
    name: momentum-scanner
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python momentum_setup_scanner.py
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: TELEGRAM_CHAT_ID
        sync: false
